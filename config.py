"""Настройки веб-версии «Киллер / Папарацци» — аналог config.py бота.

Здесь собраны все параметры, которые раньше были раскиданы по модулям. Значения-
секреты берутся из переменных окружения с безопасным дефолтом для разработки —
**в продакшене обязательно задайте `SECRET_KEY`**.
"""
import os

# --- по аналогии с ботом ---------------------------------------------------

# username'ы, которые становятся администраторами сразу при создании профиля.
DEFAULT_ADMINS = {"parar020100"}

# Путь к файлу БД (относительно папки проекта).
DB_NAME = "data/killer_game.db"

# Контакт поддержки, показывается в подвале сообщений.
SUPPORT_CONTACT = "@parar020100"

# Нужно ли жертве подтверждать поимку (пригодится при реализации ядра игры).
CONFIRM_KILLS = True

# Файл с правилами игры (пока не используется).
RULES_FILENAME = ""

# Доп. вопрос при регистрации. None = шаг отключён.
# Формат как в боте: [короткая_метка, текст_вопроса].
# Напр.: ["Комната", "Введите номер корпуса и комнаты через запятую:"]
EXTRA_INFO = None

# --- специфично для веб-версии ---------------------------------------------

# Секрет подписи cookie-сессии. В проде ОБЯЗАТЕЛЬНО задайте переменную окружения.
SECRET_KEY = os.getenv("SECRET_KEY", "dev-insecure-secret-change-me")

# Границы валидации игрового имени.
NAME_MIN_LEN = 2
NAME_MAX_LEN = 50

# Длина случайной части токена входа (secrets.token_urlsafe).
LOGIN_TOKEN_BYTES = 32

# Отладочный «вход как» через ?as=<id>: позволяет держать разных пользователей в
# разных вкладках одного браузера (обычная cookie-сессия одна на весь браузер).
# НЕБЕЗОПАСНО — даёт войти за любого пользователя без ссылки, поэтому по умолчанию
# включён только с дефолтным (небезопасным) SECRET_KEY. Явно управляется
# переменной окружения ALLOW_DEV_LOGIN=1/0.
ALLOW_DEV_LOGIN = os.getenv(
    "ALLOW_DEV_LOGIN",
    "1" if SECRET_KEY == "dev-insecure-secret-change-me" else "0",
) != "0"
