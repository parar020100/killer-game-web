# TODO — веб-версия «Киллер / Папарацци»

Единый линейный список задач. Полный эталон поведения бота — в
[FEATURES.md](FEATURES.md). Сначала — что готово, затем — что не переносим,
затем — что осталось сделать (сперва мелкие доделки, потом от важного к
второстепенному).

---

## ✅ Готово

- Каркас FastAPI + Jinja2, БД SQLite (WAL), модели `game` / `user` / `identity`.
- Мультиплатформенные идентичности (tg/vk); журнал сообщений в файлах.
- Общий журнал администраторов (`/admin-log`).
- Страница-чат (лендинг) + эмуляция переписки с ботом.
- Вход по постоянной магической ссылке + подписанная cookie-сессия.
- Отладочный «вход как» (`?user=`) — разные игроки в разных вкладках.
- `root`-пользователь вместо `DEFAULT_ADMINS`: создаётся при первом запуске,
  ссылка входа печатается в лог; через него выдают права обычным пользователям.
- Регистрация через чат; полноценная форма с валидацией (имя + пароль + доп. вопросы).
- **Несколько** доп. вопросов при регистрации; ответы видны в списке и в профиле.
- Настройки игры хранятся в БД и правятся из UI («⚙️ Настройки игры»): пароль
  регистрации, контакт поддержки, файл правил, подтверждение поимок (CONFIRM_KILLS),
  доп. вопросы.
- Настройки профиля игроком: изменить имя и ответы на доп. вопросы, вкл/выкл
  личные уведомления; для админа — вкл/выкл ADMIN LOG (уведомления о событиях игры).
- Правила игры в HTML + кнопка «Правила».
- Адресная рассылка админом по группам (`/broadcast`).
- Ядро игры: круг `game_order` + раздача целей при старте.
- Поимки: сообщить → подтвердить/отклонить → выбывание → новая цель → победитель.
- Действия админа над игроком: устранить, оживить, подарить/отобрать жизнь (очередь
  + авто-оживление), сменить порядок (случайный **и** конкретный номер), изменить
  счёт, засчитать/отклонить поимку, удалить из игры / из системы.
- Жизненный цикл: пауза / продолжить / сброс / завершить + экран итогов, топ-3,
  победитель, промежуточные результаты (`/app/results`).
- Живое обновление страниц без F5 (мини-JS поллинг).
- Единый экран-дашборд (секции вместо вкладок); шапка в две колонки (статус / цель).
- Встроенный список игроков (кнопка-переключатель, состояние сохраняется);
  компактные карточки; полный набор полей и `killed_by` в раскрытой карточке.
- Живой поиск по списку игроков (подсветка совпадений + прокрутка), без кнопок.
- Скрытая цель (кнопка-спойлер) + лента уведомлений на дашборде.
- Постоянный набор кнопок у игрока и админа: недоступные по состоянию игры —
  серые, с подсказкой по клику (почему сейчас нельзя).
- Подтверждения («вы уверены?») на всех опасных действиях.
- Выход игрока из игры в любой момент.
- Секрет-пасхалка (рикролл: выбор RuTube / YouTube).
- Перезапуск приложения и полный сброс БД из настроек.
- `ALLOW_DEV_LOGIN` — простой флаг 0/1 в `config.py`.

---

## ⛔ Не переносим в веб (специфика чат-бота)

- `/commands`, `/cancel`, реакция на неизвестную команду, кнопка «☰ Открыть меню».
- Логирование игровых событий в Telegram-группы.
- Рестарт/стоп процесса и отладочные админ-команды из чата.
- Фото-пруф поимки, лента фото-пруфов и их модерация.

---

## 🔜 Осталось сделать (сверху — простое, ниже — по убыванию важности)

- **Баг: список игроков остаётся после снятия админки.** Если у пользователя
  забрать права админа, его админские кнопки исчезают, но встроенный список игроков
  (`<aside class="userpanel">`) виден до перезагрузки — он вне области живого
  обновления (`data-live`), поэтому не убирается автоматически. Прятать список при
  потере прав (перерисовывать/убирать панель) без ручного F5.
- **Выбор файла правил выпадающим списком.** В «⚙️ Настройки игры» вместо ручного
  ввода пути — выпадающий список HTML-файлов из папки `rules/` (плюс «нет правил»).
- **Кнопка «показать / скрыть меню админа».** Для админов — прятать/показывать
  раздел управления игрой на дашборде (по аналогии с переключателем списка игроков).
- **Переход к «кем пойман» из карточки.** Клик по метке `killed_by` (☠️/⏳ + имя)
  находит и подсвечивает этого игрока в списке (переиспользовать логику живого
  поиска: подставить запрос и прокрутить к карточке).
- **Ревизия ограничений кнопок.** Проверить, нет ли лишних требований: например,
  паузу для редактирования счёта, возможно, ставить не обязательно; а для смены
  позиций в круге и изменения состава (добавить/убрать игрока) — пауза нужна.
- **Неподтверждённые поимки в списке.** Между «охотником» и «жертвой» показывать
  красную строку-заявку (как `⏳ Unconfirmed kill` в `.txt`-списке бота), сразу с
  кнопками «Засчитать» (`force_accept`) и «Отменить» (`force_deny`).
- **Личные сообщения игрок ↔ админ.** Страница переписки, кнопка «Сообщения» в
  карточке игрока (сейчас заглушка), «✍️ Написать организаторам» у игрока (сейчас
  заглушка), пересылка сообщений игрока админам.
- **Настройка режима игры «Киллер / Папарацци».** Переключатель, меняющий все
  формулировки и иконки: «устранить»↔«поймать», «убийство»↔«поимка», 🔪↔📸 и т.п.
  Тексты вынести в словарь режима, хранить выбор в настройках.
- **Выбор активной игры по файлу БД.** В настройках выбирать файл базы данных
  (= отдельная «игра»); применение требует перезапуска приложения.
- **Профиль: отписка / «забыть меня».** Удаление своей учётки и отзыв ссылки входа
  («выйти на всех устройствах») из UI.
- **Реальные боты Telegram / VK.** Заменить эмуляцию чата на настоящую доставку и
  выдачу ссылок; привязка vk/tg к одному пользователю. *(В самом конце.)*
