# TODO — веб-версия «Киллер / Папарацци»

Единый линейный список **актуальных и не доделанных до конца** задач. Уже сделанное
вынесено в [DONE.md](DONE.md). Полный эталон поведения бота — в [FEATURES.md](FEATURES.md),
соответствие сообщений — в [MESSAGES.md](MESSAGES.md).

Порядок: сначала что не переносим, затем что осталось сделать (от важного к
второстепенному). Нумерация сквозная и сохраняется (сделанные пункты уезжают в DONE.md,
их номера не переиспользуются).

---

## ⛔ Не переносим в веб (специфика чат-бота)

- `/commands`, `/cancel`, реакция на неизвестную команду, кнопка «☰ Открыть меню».
- Рестарт/стоп процесса и отладочные админ-команды из чата (`admin_bot_restart`,
  `admin_bot_stop`, `admin_list_debug` — авто-обновляемый список, `admin_set_password_help`).
- Логи в TG-группы (`admin_group_enable_log` и др.) — заменяются каналами уведомлений.

---

## 🔜 Осталось сделать (средний приоритет)

15. **Двусторонние сообщения игрок ↔ админ.** Сейчас есть только одностороннее
    сообщение админ→игрок. Нужно: вернуть кнопку «✍️ Написать организаторам» у игрока
    (временно убрана) и пересылку сообщений игрока админам; полноценная страница
    переписки.
30. **Переделать сообщения режима «Киллер».** Вернуть «убийства» вместо
    «устранений»/«поимок» — привести формулировки киллера к исходному стилю бота.
31. **Ревью всех сообщений игры.** Сверить все тексты уведомлений/логов с
    аналогичными из тг-бота (единый стиль, ничего не упущено).
37. **Показ фото** *(в работе).* Кнопка «фото убийства/поимки» в строках с
    неподтверждёнными поимками и в меню устранённого игрока; работает только когда
    фото есть и фото-пруф включён.
41. **Шаблоны для развёртывания** *(в работе).* Отдельная папка `deploy/` с готовыми
    скриптами установки, файлом `killer.service` и т.д.; инструкция разбита на шаги.
42. **Полноценный игровой лог** *(в работе).* Два лога в файлах: лог бота и лог
    игровых событий (+ все действия пользователей и ошибки бота). В настройках —
    кнопки открыть оба лога сырым текстом (в новой вкладке). В меню админа — два
    раскрывающихся окошка: «уведомления» → «уведомления игрока», и «лог игры».
    Интерфейс: на достаточно широком экране (три столбца) справа показывать лог игры,
    скрывать/показывать кнопкой слева (как список игроков).
45. **Админы по умолчанию по платформам.** Вернуть админов по умолчанию, задаваемых
    в `config.py`, ОТДЕЛЬНО для Telegram и VK: один и тот же человек может быть
    админом при входе через VK и не быть при входе через TG (и наоборот). Указывать
    можно числовой id или username/screen_name канала.

Выполненные пункты 38, 39, 40, 43, 44 — в [DONE.md](DONE.md).

---

## 🔽 Осталось сделать — низкий приоритет

11. **Ревизия ограничений кнопок.** Проверить, нет ли лишних требований: например,
    паузу для редактирования счёта, возможно, ставить не обязательно; а для смены
    позиций в круге и изменения состава (добавить/убрать игрока) — пауза нужна.
18. **Логирование игровых событий в группы Telegram / VK.** *(Может быть.)* Слать
    события игры в привязанные чаты/группы. Зависит от реальных ботов.
23. **Очистка временных файлов (`make clean`-аналог).** Способ убрать временные
    файлы бота — скриптом (а может и `Makefile`, решить позже): логи сообщений,
    кэш `__pycache__`, WAL-хвосты БД, скачанные фото-пруфы и т.п.
27. **Отдельные папки фото и историй чата на каждую игру (файл БД).** Сейчас
    `data/photos/` и `data/chats/` общие; при нескольких играх (файлах БД) их нужно
    разделять по игре, чтобы данные разных мероприятий не смешивались.
28. **Переносить ссылки доступа при смене базы данных.** При переключении активной
    игры (файла БД) сохранять/переносить постоянные ссылки входа текущего
    пользователя, чтобы после перезапуска можно было просто обновить страницу и
    остаться в системе (а не входить заново).
33. **Исправить кнопку «скрыть / показать пользователей»** — она периодически до сих
    пор работает некорректно при переключении статуса админа.


---

## 🔽 Осталось сделать — добавить и пронумеровать (по-умолчанию в средний приоритет)
(этот раздел удалять не надо)

<!-- Новые идеи добавляйте сюда как маркированный список; при разборе им присваивают
     следующий свободный номер и переносят в разделы выше. -->
- Можно ли сделать так, чтобы сообщения "игра поставлена на паузу" и "игра продолжается" отправлялись пользователям без звука?
- Разделить настройки конкретной игры и настройки бота на странице "Настройки игры" (страница остаётся одна, но два раздела друг над другом)

